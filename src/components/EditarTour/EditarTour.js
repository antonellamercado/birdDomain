import {useState} from 'react';
import {Button, Modal, ModalHeader, ModalBody, ModalFooter, Form, FormGroup, Label, Input, Row, Col} from 'reactstrap';

const EditarTour = () => {
    
    return(
        <Button disabled>Editar Tour </Button>
    );
}
export default EditarTour